{
  "name": "pentaloom-electron",
  "private": true,
  "version": "0.1.0",
  "description": "PentaLoom 桌面壳 (Electron Main + Preload). M15: dev-only shell.",
  "main": "out/main/main.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && electron ."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "electron": "^40.1.0",
    "typescript": "^5.9.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron"
    ]
  }
}
